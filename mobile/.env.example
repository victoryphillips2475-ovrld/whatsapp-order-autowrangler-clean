# API base URL — use Vite proxy in development, set explicit origin in production
# Development: leave empty to use Vite proxy (/api/v1 → http://localhost:8000)
# Production: set to your deployed backend URL e.g. https://api.yourdomain.com
VITE_API_URL=