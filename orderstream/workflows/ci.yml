name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r orderstream/requirements.txt
      - name: Run lint
        run: |
          pip install flake8
          flake8 orderstream/backend
      - name: Run tests
        run: |
          echo "No tests defined"
